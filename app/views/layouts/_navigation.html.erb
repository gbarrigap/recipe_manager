<nav class="navbar navbar-fixed-top navbar-default navbar-inverse">
  <div class="container-fluid">
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "myRecipes", root_path, class: "", id: "logo" %></li>
        <li><%= link_to "link_1", "", class: "" %></li>
        <li><%= link_to "link_1", "", class: "" %></li>
        <% if logged_in? %>
        <li><%= link_to "New Recipe", new_recipe_path, class: "" %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.chefname %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'View Profile', chef_path(current_user) %></li>
            <li><%= link_to 'Edit Profile', edit_chef_path(current_user) %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to 'Log Out', logout_path %></li>
          </ul>
        </li>
        <% else %>
        <li><%= link_to 'Log In', login_path, class: '' %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
